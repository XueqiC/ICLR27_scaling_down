__pycache__/
*.pyc
# The paper source now lives in its own repository (XueqiC/Scaling_down_law_paper).
# `paper/` here is the local clone of that repository (its own git repo); never track it.
/paper
