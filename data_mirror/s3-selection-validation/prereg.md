# S3 independent cross-method selection: preparation preregistration

**Status: BLOCKED; preparation sealed, final numeric prediction freeze incomplete. No jobs launched; no commit.**

The five proposed slots and twelve candidates per slot are fixed below. Pythia-410M/1.4B step 143k are excluded because Appendix H's actual development rosters include them. Step 120k replaces those proposals. Pythia-2.8B step 143k is a conditional slot, not an admitted independent reference: the recorded cache has identical learned tensors at 16k/64k/143k, and LONI lacks this checkpoint. A fresh download must pass tensor-level identity checks against all excluded revisions before admission. No four-reference fallback is selected after outcomes.

**Exclusions.** plan.json lists the union of the frozen predictor's development roster, historical KD states, V78 confirmation states and additional known test states. New students must start from pristine base weights, train in a new S3 directory and provide new evals. No A12 adapter, checkpoint or evaluation enters S3 candidates or fitting. Shared trace corpora/probe texts are protocol inputs, not independent outcome replications.

**Candidate scope and storage.** Each reference has dense, d=.9/.8/.7/.6 pruning; per-channel 8/6/4-bit symmetric RTN; grouped (b,g)=(4,32),(4,128),(3,32); and one new small LoRA student. Storage is nominal text-decoder matrix storage relative to the dense reference: d; b/16; (b+16/g)/16; Ns/Nref for a merged student; 1 for dense. This is Section 7's convention, not measured file size, memory, sparse index cost, or end-to-end deployment bytes. The g=32 predictions extrapolate the existing locked group-boundary rule without refitting.

**Budgets and feasibility.** B = .20,.25,...,1.00 (17). Candidate feasible iff r<=B+1e-12, equally for both selectors. Dense enters only at B=1. Quant-only uses precisely the six quantization entries of the same candidate table, the same predictions and dense input information, and the same tie priority as V64 (method order prune/quant/distill/dense, then r, then ID). The smallest quant candidate uses 21.875%, so quant-only and O are undefined at 20%; null is never zero. Primary paired means are predeclared over 25–100% (16 budgets) for every reference. The 20% cells remain in all maps and feasibility reports, including valid rule-only results. No missing measurements may shrink this denominator.

**Targets and endpoints.** MATH-500 math, MBPP code, 2Wiki QA, and multi=max_c(Lc(candidate)-Lc(reference)). Use the exact frozen V6 128 probes/seed 0, odd half 64 per capability, completion-token-weighted CE in native-token nats. Reference and student tokenizers must match within each comparison. Report each reference and family separately; no Pythia/Gemma token pooling or shared nats average. 'Largest increase' is a maximum before oracle minimization.

**Frozen predictor.** Copy the V78 independent freeze's `models.locked` verbatim, with hashes, and evaluate analysis/final_rule.py. New-state pruning/channel/grouped branches use source-free development medians plus reference dense loss. The historical KD branch is NOT wholly source-free: math uses Ns, student dense loss and Ds; code/QA use frozen arithmetic means. Its domain is smaller same-stage students trained with pool 600/two epochs. The requested corner recipe (U=200/100k tokens) is outside that training protocol, and Gemma has no registered same-stage Ds. Substituting zero, invented Ds, A12 fits, or dropping KD would change the question. These cells remain explicitly blocked pending a pre-outcome protocol resolution. plan.json enumerates all 180 candidate/capability predictions as exact anchor expressions or explicit blocked KD expressions, and all 340 reference/target/budget selection records with both policies. Numeric absolute losses are null until legal anchors exist. Quant-only selections are already fixed wherever feasible because common anchors cancel. Full all-method maps are not falsely labeled frozen.

**New student recipe.** gpt-5.6-luna trace teacher, full recipe, math/QA/code, U=200 source examples per domain (the existing V12 pool convention), data seed 0 and training seed 0. Strict LoRA r=16, alpha=32, dropout=0, bias=none; family-specific existing V12 attention/MLP targets; LR=1e-4, AdamW, cosine, 3% warmup, effective batch 16, max length 1024, bf16, 60-epoch ceiling and fixed 678000 processed-token schedule. CPU replay chooses the completed update closest to 100000 supervised completion tokens (ties earlier), recording the actual count and relative error. V12's trajectory flag receives the corresponding PROCESSED-token threshold, never the number 100000 mislabeled as supervision. The registered tolerance is 0.5%, following corner accounting; if the fixed pool cannot attain it the slot stays blocked, with no pool search or altered budget. All five commands and exact pool/tokenizer hashes are in plan.json. Same deployment reference does not assert a teacher–student pair.

**Diagnostics and decision.** O_cB=min_quant Lc − min_all Lc; R_cB=Lc(selected)−min_all Lc, over feasible candidates. Quant-only regret uses its frozen prediction-selected candidate, not the measured best quant candidate; it equals O plus within-quant selection error. Compute per reference/target/budget, report O separately, and support a reference iff mean rule regret <= mean quant-only regret over its complete 4 targets × 16 paired budgets (no fitted margin). Report target-specific decisions too. Feasibility rate uses all 17 budgets; constraint satisfaction is selected candidate meeting nominal storage, with both unconditional and conditional-on-selection denominators. No capability loss threshold was specified, so there is no invented loss-constraint pass rate. Missing, nonfinite, mismatched or unsealed outcomes make the reference pending/invalid, not supported. Family reports list reference decisions; a grand mean across families is forbidden.

**Execution order.** Resolve the recorded domain/identity blockers; verify fresh snapshot tensor fingerprints and tokenizer identity; measure only pristine reference/student dense anchors under the frozen probes; seal every numeric prediction and both full maps before any S3 student training or transformed-model forward. Preserve this preparation and register any amendment before outcomes. The provided SLURM jobs enforce `launch_ready` and full-map completeness before loading weights. They therefore cannot run this blocked plan. Preparation hashes are not a claim that the missing final freeze happened. User instructed no commit, so SHA256 files replace the plan note's commit step for this preparation only.

**Current blockers:**

- No final dense reference/student anchor measurements or numeric full-map freeze exist.
- Requested 410M/1.4B step143000 states are excluded; step120000 replacements need immutable snapshot verification.
- 2.8B step143000 remains conditional: recorded tensors identical to excluded revisions; absent on LONI.
- Locked KD recipe (pool600/two epochs) does not cover the mandated new corner recipe (pool200/100k supervision).
- Locked KD math requires Ds; Gemma has no registered same-stage pretraining Ds. No replacement predictor fitted.

**Seals:** `plan.json.sha256` seals the plan bytes; `SHA256SUMS` seals plan, preregistration, tables, opportunities, inputs and code. `predictions_sha256` and `maps_sha256` in plan.json independently seal their canonical JSON values. A changed sealed input invalidates execution and scoring.
