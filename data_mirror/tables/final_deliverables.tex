\begin{table}[H]
\centering\scriptsize\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.05}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{1.5cm}>{\raggedright\arraybackslash}p{5.6cm}>{\raggedright\arraybackslash}p{3.3cm}>{\raggedright\arraybackslash}p{3.4cm}@{}}
\toprule
Arm & Delivered predictor & Inputs & Not applicable to \\
\midrule
Pruning & Math and code at unseen $d\in[0.6,0.9]$ of states in the fit: power form of Eq.~\ref{eq:power}. New sizes or stages, and QA: the frozen median development curve $L_{0,c}+\widetilde f_c(d)$. & $N_0$, $D_0$, $L_{0,c}$ (source forms); $L_{0,c}$ only (median curve) & $d\le0.55$; QA source terms; source-conditioned forms on new sizes \\
\addlinespace
Quantization & Per-channel on new states: per-bit development median. Grouped on seen states: same-input piecewise interpolation over the measured $(b,g)$ grid, linear in $\log_2 g$ (math, code); per-configuration median (QA, and every capability on new states). & Measured development grid; $N_0$, $D_0$, $L_{0,c}$ for the interpolation & The parametric surface; source conditioning on new states; QA \\
\addlinespace
Distillation & On development students: $\widehat\delta_c=a_c\log(1+E)$ (math, code) and the joint budget-pool form (QA); reference is the initial student. & $T$, $D_U$ ($E=T/D_U$); $L_{0,c}$ of the student & Student size (4B); QA outside the 2Wiki distribution; reuse counts near 14 \\
\bottomrule
\end{tabular}
\caption{The relation delivered for each compression method, the inputs it reads and the settings it does not cover. Errors for these relations, their frozen candidates and their development baselines are in Table~\ref{tab:main-prediction-v2}.}
\label{tab:final}
\end{table}
