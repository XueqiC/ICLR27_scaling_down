\section{Experimental Details}
\label{sec:app_details}
\subsection{Probes and loss computation}
\label{sec:app_probes}
Each capability is measured on a fixed probe set of prompt--reference
pairs drawn once, with a fixed seed, from held-out benchmark splits that
are disjoint from each training and recovery source used in the paper.
The primary measurement benchmarks are MATH-500 for mathematical
reasoning, MBPP for code generation, and 2WikiMultihopQA for multi-hop
question answering. For each capability we draw 128 probes and split
them by index parity into an estimation half, used only for gradient
and Fisher statistics, and a measurement half of 64 probes on which
each reported loss is computed, so that mechanism statistics and
evaluations do not share sampling noise. Secondary benchmarks used for the measurement checks below are
GSM8K, HumanEval, and HotpotQA on the pruning panel (no training
involved) and SVAMP, HumanEval, and TriviaQA where distilled models are
scored, chosen to exclude the benchmarks from which the distillation
traces were derived; a sample-level audit found no overlap between any
measurement probe and any training pool. The measurement half is 64
probes per capability for every result in the paper except where a
512-probe re-measurement is stated.


\subsection{Compression protocols}
\label{sec:app_compress}
\paragraph{Pruning.} Global magnitude pruning removes the smallest
weights across all transformer matrices (attention and MLP projections;
embeddings, output head, biases, and norms are excluded) to a target
retained density $\dens$. The global threshold is estimated from a
seeded sample of absolute weights so that the same procedure scales to
30B-parameter models; each density is applied to the same dense
reference weights, which are held on the CPU for the largest models so
that a single 80~GB device suffices.

\paragraph{Quantization.} Weights are fake-quantized per output channel
with symmetric round-to-nearest at bit-width $\bits$, using the integer
range $[-(2^{\bits-1}-1),\,2^{\bits-1}-1]$ and a per-channel scale set
by the channel's maximum absolute value; activations are left in
bfloat16. The dense weights are restored between bit-widths. The quantizer is
\texttt{fake\_quantize\_per\_output\_channel} in the released code:
$q_{\max}=2^{\bits-1}-1$ is the largest positive integer, integers are
clamped to $[-q_{\max},q_{\max}]$ ($2^{\bits}-1$ values), and the
scale is the channel's maximum absolute weight divided by $q_{\max}$.
With a fixed clipping range the step size from 4 to 5 bits shrinks by
$7/15$, so the squared step ratio is $(7/15)^2\approx0.22$; in general
the squared step ratio between adjacent bit-widths is
$(q_{\max}(\bits)/q_{\max}(\bits+1))^2$, not a constant $1/4$. A
squared step ratio becomes a loss ratio only under assumptions about
the error distribution and the local response that we do not test, so
the fixed $4^{-\bits}$ shape is one candidate among others rather than a
derived reference. Nominal storage ratios are computed from bit-widths;
no memory or latency was measured.

\paragraph{Distillation.} Students are fine-tuned on black-box teacher
traces (a frontier API teacher answering GSM8K, HotpotQA, and CodeAlpaca
prompts; 600 traces per domain) under the full-trace recipe, with
strict LoRA adapters of rank 16 and scaling 32 on the query, key, value,
output, gate, up, and down projections, AdamW at learning rate
$10^{-4}$, an effective batch of 16 sequences, and a cosine schedule
with 3\% warmup. A run aborts if the adapter library is unavailable and does not fall
back to full fine-tuning silently, and the actual
training mode and trainable parameter count are recorded in each
manifest. Token accounting distinguishes processed tokens (all input
tokens seen, including repetitions), unique pool tokens, and supervised
completion tokens. The data pool of $U$ examples per domain is either
the first $U$ rows of the trace file (the historical protocol) or a
random sample drawn with a pool-sampling seed that is independent of the
training seed; pools are identified by the sorted hashes of their
samples and pairwise overlaps are reported. The historical schedule
spanned a fixed number of epochs, so its cosine horizon varied with
pool size; the controlled matrix of \S\ref{sec:law_distill} uses a schedule
defined on the same absolute processed-token budget for each pool, and
the two protocols are kept apart in fitting.



\paragraph{Grouped quantizer.} The grouped variant (\S\ref{sec:law_quant}) splits each row of a weight matrix into
contiguous groups of $g$ input weights, pads the last group with zeros (which do not affect the group's absmax), and
applies the same symmetric round-to-nearest with $q_{\max}=2^{b-1}-1$ per group; $g$ equal to the row width reproduces
the per-channel quantizer bit-exactly (verified by a unit test). Configurations are $b\in\{3,4,5\}$ and
$g\in\{64,128,256\}$; nominal storage is computed from $b$ and $g$, and no memory or latency was measured.

\subsection{The controlled Pythia panel}
\label{sec:app_pythia}
The panel uses the public Pythia checkpoints at sizes 160M, 410M, and
1.4B and pretraining steps 16{,}000, 64{,}000, and 143{,}000, which
correspond to 33.6B, 134.2B, and 299.9B processed tokens at the
published batch of $1024\times2048$ tokens per step. The size
coordinate $N_0$ counts transformer-matrix parameters only (the query,
key, value, output, and MLP matrices of each layer), computed from the
architecture configuration, and the training-tokens coordinate is
$D_0=\text{step}\times 2{,}097{,}152$. Each checkpoint's weight identity
was verified before use: the 2.8B revisions published for different
steps were found to load identical weights and were excluded, and an
integrity guard now rejects any pair of nominally different checkpoints
with identical measured panels. New sources added for the prospective
tests (Pythia-1B at steps 32{,}000, 96{,}000, and 112{,}000;
Pythia-6.9B at 32{,}000 and 112{,}000) were confirmed to have not
entered any fit or model selection before their predictions were
frozen.

\FloatBarrier
The three checkpoints of the confirmation panel (410M@48k, 1.4B@112k, 6.9B@80k) were verified as distinct weights
before use: their model-shard hashes differ from every other cached revision of the same size, and their dense
capability vectors differ from those of the neighbouring stages.

\begin{table}[p]
\centering\footnotesize\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{2.55cm}p{3.35cm}p{2.95cm}p{1.85cm}p{2.35cm}@{}}
\toprule
Cohort & Sources & Interventions & Probes & Role \\
\midrule
Heterogeneous panel & 12 dense checkpoints, \nSeries{} series from
\nFamilies{} families: Qwen3 0.6B/1.7B/4B, Gemma-3 270M--27B,
Gemma-4-31B, Muse-30B, OLMo-3 7B/32B (+2 prospective: Qwen3 8B/14B) &
global magnitude pruning, 10 densities in $[0.3,0.9]$; RTN int8/6/5/4/3
& 64 per capability, primary benchmarks & phenomena; cross-family
transfer; noise screen \\
Controlled Pythia panel & sizes 160M/410M/1.4B $\times$ steps
16k/64k/143k (9 states); new sources 160M, 410M, 1.4B at 96k; 1B at
32k/96k/112k; 6.9B at 32k/112k & pruning: 4 dev densities 0.9--0.6,
tests at 0.75/0.65/0.55; bits 8/6/4/3, test 5 & same & development
set; source-axis and configuration-axis tests \\
Distillation, controlled & the 9 Pythia states as students; fixed
teacher pool (600 traces per domain); LoRA & fixed recipe & same &
$\delta_c$ source transfer \\
Distillation, pools & Gemma-3-1B student; pools U75 and U600 (fit),
U225 (test); 3 pool seeds $\times$ 2 training seeds & processed tokens
$T$; reuse count $E=T/D_U$ & same & configuration axis (pool) \\
Distillation, heterogeneous & Gemma-3 270M--12B, OLMo-3-7B, Qwen3
0.6B--4B students; two API teachers; full-trace vs answer-only & recipe
& same & phenomena only \\
\bottomrule
\end{tabular}
\caption{Cohorts and their roles. Counts are generated from the result
files (\texttt{v51\_panel\_tables.py}); the two Qwen3 prospectives were
measured at four densities only. The 2.8B Pythia checkpoints were
excluded because the step revisions we downloaded loaded identical
weights (hash check); nothing is claimed about other revisions.}
\label{tab:panels_full}
\end{table}

\section{Fitted Models and Coefficients}
\label{sec:app_coef}
The delivered pruning form (Eq.~\eqref{eq:power}) and its standardization are in Table~\ref{tab:round3_coef}; the
grouped-quantization form (Eq.~\eqref{eq:quant2d}) in Table~\ref{tab:quant2d_coef}; the per-bit source regressions,
the distillation source form and pool forms, and the frozen multi-student forms are stored with their standardization
constants in the register files listed in Appendix~\ref{sec:app_registration}.
\input{tables/round3_coef}
\input{tables/quant2d_coef}

\section{Alternative Functional Forms and Model Selection}
\label{sec:app_forms}
\subsection{Pruning: expanded development set and the selection rule}
\input{tables/v53_loso}
\subsection{Grouped quantization: leave-one-state-out on six states}
\input{tables/v55_loso}
\subsection{Distillation: descriptor forms and capability conditioning}
\input{tables/v56_forms}

\section{Complete Prediction Results}
\label{sec:app_results}
Tables~\ref{tab:pred_source}--\ref{tab:pred_config_qd} give every registered test in three lines (candidate,
strongest same-input baseline, strongest simple baseline) with improvements and provenance codes;
Table~\ref{tab:p1v2} the per-stage, per-protocol results of the Pythia-1B and 6.9B pairs; Tables~\ref{tab:round3_prune}
and \ref{tab:round3_quant} the confirmation panel and the grouped-quantization tests; Fig.~\ref{fig:gains} the paired
improvements behind Fig.~\ref{fig:inputs}.
\input{tables/pred_source}
\input{tables/pred_config_prune}
\input{tables/pred_config_qd}
\subsection{Pythia-1B and 6.9B pairs under two protocols}
\label{sec:app_p1v2}

The second frozen round (Table~\ref{tab:p1v2}) added seven pruning
densities (0.9, 0.8, 0.75, 0.7, 0.65 and 0.6 as interpolation, 0.55 as
extrapolation) and five bit-widths (8, 6, 5, 4, 3) on Pythia-1B at
steps 32k and 112k. Two fitting protocols were frozen at the same time.
Protocol A fits each candidate on the full nine-state development
panel. Protocol B fits on development states at step 64k or below, so
that its 112k rows test a training-stage extrapolation on top of the
size change. The 5-bit prediction of the class-indicator candidate is a
fixed rule, the average of its committed 4- and 6-bit predictions,
chosen so that no 5-bit measurement enters any fit. Candidate names
follow \S\ref{sec:law_prune}: power is the shared power form, A2 the
per-density source regression with a fixed interpolation rule, A1 its
$\gamma=1$ variant, cont a continuous two-term polynomial in $1-d$, and
the three source-free baselines are strength-only, the median
development curve, and zero change. For quantization, full uses
$\{N_0, L_0, D_0\}$, no-D0 drops the training-stage term, and mean and
median are per-bit constants from the development panel.

Per capability, at 112k under protocol A the source regression reaches
MAE 0.111 (math), 0.091 (code) and 0.135 (QA); the strongest
source-free alternative is strength-only for math (0.117) and the
median curve for code (0.107) and QA (0.261). At 32k the QA error of
the source regression rises to 0.339 while the median curve stays at
0.193, which accounts for most of the aggregate gap. Under protocol B at
112k the power and A2 forms are ahead of strength-only at $d=0.55$
(0.96 and 1.00 against 1.33), but with errors near one nat the ordering
carries little practical value. The pair cost 22 A100 minutes.

The 6.9B pair, a size extrapolation of about five times, was measured
under the same freeze (36 A100 minutes, reference model on CPU). Its
dense anchors are math 1.27, code 1.30 and QA 5.11 at 32k, and 1.11,
1.21 and 5.09 at 112k. Per capability under protocol A at 112k, the
source regression reaches 0.23 (math), 0.19 (code) and 0.96 (QA)
against 0.05, 0.07 and 0.15 for the median curve. Under protocol B the
source forms lead on code only (A2 0.058 and 0.083 at the two stages
against 0.208 and 0.074 for the median curve) and trail on QA (0.32 and
0.20 against 0.08 and 0.15). At $d=0.55$ the source-free curves win
under both protocols, with source-conditioned errors between 0.55 and
2.2 nats. For quantization under protocol A the class-indicator
candidate is worst or near worst above 3 bits (0.25 and 0.27 against
0.03 and 0.04 for the per-bit median) and overshoots by about four nats
at 3 bits. Protocol B compresses the differences above 3 bits to the
0.02 to 0.05 range and leaves the candidate ahead at 3 bits only for the
late stage (1.29 against 3.8 to 4.1).




\input{tables/p1v2}
\input{tables/round3_prune}
\input{tables/round3_quant}
\begin{figure}[p]
\centering
\includegraphics[width=0.92\textwidth]{figs/input_form_gain.pdf}
\caption{Paired improvements, zero line marked, positive means the first-named model is better. A: full model against its no-$D_0$ variant, per arm and capability; intervals only where a cluster bootstrap was computed (leave-one-stage-out panels), $n$ = evaluated source states. B: source-conditioned candidate against the strongest source-free baseline for all twenty registered tests (hollow = frozen prospective, filled = leave-one-out). C: shared power form against the same-input alternatives A1 and A2 on the pruning configuration tests. Generated by \texttt{plot\_fig2\_gains.py} from \texttt{v52} groups and the v36b/v39/v42/v46/v49 result files.}
\label{fig:gains}
\end{figure}

\FloatBarrier
\section{Capability-Loss Measurement Checks}
\label{sec:app_measure}
\paragraph{Capability specificity as a prediction test.} On a
six-model, five-density pruning panel ($d$ from $0.9$ to $0.7$), the
response on the primary benchmark of each capability was used to
predict the response on the secondary benchmark of the same capability
through a two-parameter affine map fit out of fold, and compared with
the best cross-capability predictor chosen the same way. With whole
models held out, the same-capability gain is $+0.04$ nats for math
(95\% model-bootstrap interval $[0.03,0.05]$) and $+0.14$ for QA
($[0.02,0.27]$); with whole densities held out it is $+0.14$
($[0.03,0.30]$) and $+0.21$ ($[0.02,0.52]$). For code the
cross-capability predictor is as good or better ($-0.06$,
$[-0.16,-0.00]$ with densities held out), so code specificity is not
established at this resolution (Fig.~\ref{fig:measurement}). Six model clusters give descriptive
intervals only.



\paragraph{Loss and behavior.} Loss-to-accuracy links were fit on
GSM8K accuracy, MBPP pass@1, and TriviaQA exact match and token F1
across seven pruned models; all four outcomes are compatible with one
cross-family sigmoid in the loss, with no resolved family-specific
term. A lower QA loss does not by itself imply better QA behavior: the
four pruned OLMo-3-32B cells whose QA loss lies below dense show no
increase in exact match. This audit covers pruning only.

\paragraph{Reference conditions.} For a subset of models the
direct-answer and given-reasoning-answer conditional distributions were
measured in addition to the full-solution reference; they differ from
it for math and QA and coincide for code, where the reference is the
function body, so the numbers in this paper are specific to the
full-solution reference and are not evidence of format robustness.

The loss is the conditional cross-entropy of the reference completion
given the prompt, summed over completion tokens and divided by the
number of completion tokens, as in Eq.~\eqref{eq:taskloss}. Prompts and
references are tokenized with the model's own tokenizer and truncated to
1024 tokens; prompt tokens are masked out of the loss. All within-model
responses $\Delta L_c$ are differences in native-token nats
on identical token sequences. Because native-token units differ across
tokenizers, cross-family comparisons of absolute levels use per-byte
normalization or are reported separately, and no cross-tokenizer
quantity is treated as a common numerical scale. Negative responses are
retained as measured; nothing is clipped at zero.

\FloatBarrier
\begin{figure}[!t]
\centering
\includegraphics[width=0.95\textwidth]{figs/measurement_support.pdf}
\caption{Measurement support for capability specificity. Top: out-of-fold MAE of predicting the secondary-benchmark response of each capability from the primary-benchmark response of the same capability (diamond) or of another capability, against zero and train-mean controls, on the six-model, five-density pruning panel; rows are hold-out protocols with all densities and with $d\ge0.75$ only. Bottom: paired same-over-cross gain with 95\% model-bootstrap intervals (six clusters, fixed out-of-fold fits). Generated by \texttt{plot\_fig4\_measurement.py} from \texttt{v26-loss-validity-pred/summary.json}.}
\label{fig:measurement}
\end{figure}

\FloatBarrier
\section{Heterogeneous-Model Results}
\label{sec:app_hetero}
\input{tables/panel_prune}
\input{tables/panel_quant}
\paragraph{Pruning.} Table~\ref{tab:panel_prune} gives per-capability
damage at two densities, the density at which math damage first exceeds
one nat, and how QA ranks before that cliff. Magnitudes differ by two
orders across series at the same density (OLMo-3-32B $+0.01$ against
Gemma-3-27B $+4.0$ on math at $d=0.7$). The ordering of capabilities is
series-specific: in the Qwen3 sizes and in OLMo-3-32B, QA is the
least-damaged capability at every pre-cliff density and improves, by up
to $-0.79$ nats at Qwen3-4B; in every Gemma-3 size, in Gemma-4-31B,
and in Muse-30B, QA is the most-damaged capability at the mildest
densities (Gemma-4-31B: $+2.89$ QA against $+0.68$ math at $d=0.7$);
OLMo-3-7B lies in between with differences of at most $0.15$ nats. A
noise screen sets the scale: the median $|\Delta L|$ at $d=0.9$ is
$0.013$ nats, and 29 of the 62 nominal improvement cells across all
arms exceed twice that median; the rest are treated as zero. Cliffs
occur between $d=0.5$ and $0.7$ for Qwen3 and Gemma, at $0.55$ for
Muse-30B and $0.35$ for OLMo-3-7B, and had not occurred by $d=0.3$ for
OLMo-3-32B. Robustness is not monotone in size within a series
(Gemma-3-12B is the most robust Gemma-3 size; 27B collapses earlier).
We read this as heterogeneity a predictor has to reproduce, not as an
invariant.

\paragraph{Quantization.} Table~\ref{tab:panel_quant} covers
14 models. Int8 and int6 stay within $0.01$ nats of dense. Int4
damages small models (Gemma-3-270M $+2.0$, $+2.4$, $+0.9$) and barely
touches large ones (Gemma-3-27B $+0.33$, $+0.23$, $-0.67$); a QA
improvement under int4 recurs in five models and clears the noise
screen. Int3 collapses 12 of the 14 models by $4.6$ to $28$ nats;
OLMo-3-7B loses $2.8$ to $4.7$ and OLMo-3-32B only $0.4$ to $1.2$, so
the collapse is common but not universal, and OLMo-3 is the robust
series under both interventions. Within the tested bit-widths a learned
exponential $q_c e^{-kb}$ fits held-out bits better than the fixed
$q_c4^{-b}$ shape (pooled MAE $6.7$ against $7.6$ nats), which says the
fixed base is wrong for this quantizer and nothing more; the squared
step-size ratio between 4 and 5 bits is $(7/15)^2\approx0.22$
(Appendix~\ref{sec:app_details}), and a loss ratio would follow only
under error-distribution assumptions we do not test.

\paragraph{Distillation.} Black-box traces from two API teachers (600
per domain) train Gemma-3 students under a full-trace and an
answer-only recipe in a paired design. The answer-only recipe hurts
math more ($+0.19$ to $+0.22$ against $+0.09$ to $+0.12$ nats relative
to the initial student) while QA improves under every recipe ($-0.58$
to $-1.44$); because the recipes differ in supervised tokens and update
counts, this pairing does not isolate reasoning coverage, and the
controlled comparisons are those of \S\ref{sec:law_distill}.

\paragraph{What the endpoint does and does not say.} The same
heterogeneity appears on independent secondary benchmarks for math and
QA, where the primary-benchmark response predicts the secondary one
better than any other capability's response does, with intervals
excluding zero in most hold-out protocols; for code the
cross-capability predictor is as good or better
(Appendix~\ref{sec:app_measure}, Fig.~\ref{fig:measurement}). A lower
QA loss does not by itself mean better QA behavior: in the four pruned
OLMo-3-32B cells whose QA loss lies below dense, exact-match accuracy
does not rise. ``QA improves'' in this paper refers to the loss.


\subsection{Scale and family structure}
\label{sec:family}

\paragraph{Within-family robustness is non-monotonic in two families
independently.} Gemma-3 robustness peaks at 12B (27B collapses far
earlier, $+4.0$ vs.\ $+0.68$ at $\dens=0.7$), mirroring the prior
ability-space observation of a Qwen3 14B peak. A mid-scale robustness
peak is a reproducible pattern of scaling-down.
\paragraph{A candidate predictor, honestly retired.} The
top-concentration statistic $\kappa_c$ tracked robustness within the
Qwen3 ladder and was pre-registered as a predictor; the full grid
retires it (cross-family inversion: OLMo, the most robust family,
has the lowest concentration; within-Gemma top-end violation).
\paragraph{Matched-size panel ($\sim$30B).} At matched scale the
family coefficient dominates: $\Delta\Lc{\text{math}}$ at $\dens=0.7$
spans $+0.02$ (OLMo-3-32B), $+0.17$ (Muse-30B), $+0.68$ (Gemma4-31B),
$+4.04$ (Gemma3-27B). OLMo-3-32B survives $70\%$ pruning with
$\le+0.63$ nats on each capability. Family identity is a first-order determinant of prunability on this panel; which training-data or recipe properties carry it was not measured.

\FloatBarrier
\section{Mechanistic Analyses}
\label{sec:app_mech}
\subsection{Capabilities occupy distinct regions of gradient space}
\label{sec:regions}

\paragraph{Convergent validity (block structure).} For each of nine
benchmarks we measure a gradient signature (the benchmark-conditional
diagonal Fisher) and compare all pairs. After removing the component
shared across benchmarks, signatures of same-capability benchmarks are
positively aligned (cosine $+0.36$ to $+0.45$) while signatures of
different capabilities are \emph{anti-aligned} ($-0.23$ to $-0.25$),
in each of the ten models from five series for which the block analysis was run
(Figs.~\ref{fig:blocks}--\ref{fig:blocks_olmo3_7b}); the C4 control sits between blocks.
\preliminary{point estimates; example-bootstrap, permutation, and
prompt-stability tests are left to future work} The
mathematics benchmarks cluster regardless of format, and within-block
fine structure matches intuition (SVAMP, grade-school word problems,
is the loosest member of the math block). A projected-gradient SVD
version gives the same verdict in subspace terms: principal-angle
similarity within capability exceeds cross-capability similarity by
$2$--$4\times$.

\input{figs/appendix_blocks}

\paragraph{Selective ablation (preliminary evidence: single model; random, magnitude-matched, and global-Fisher controls are left to future work).} A first targeted test:
select the top $0.05\%$ of weight coordinates by each capability's
\emph{residual} Fisher signature (excluding coordinates shared with
other capabilities) and zero them. The damage matrix is nearly
diagonal (Gemma-3-1B; rows ablate, columns measure, nats/token):

\begin{center}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
Ablated region & math & code & QA \\
\midrule
math & $\mathbf{+4.63}$ & $+0.43$ & $+0.05$ \\
code & $+0.42$ & $\mathbf{+7.14}$ & $+0.00$ \\
QA   & $+0.01$ & $+0.00$ & $\mathbf{+2.29}$ \\
\bottomrule
\end{tabular}
\end{center}

Zeroing $0.05\%$ of weights, selected from gradients alone, selectively
removes one capability while leaving the others nearly untouched
(the QA ablation moves code by $+0.0001$). Pending multi-model
replication and the control ablations above, we describe the regions as
causally implicated and partially separable; we do not call them proven
causal carriers; if confirmed, ablation itself is a form of
\emph{targeted} scaling-down.

\FloatBarrier
\subsection{The pruning law}
\label{sec:law}

The mechanism form below fits the entire pre-cliff grid.
With the measured alignment term subtracted, the curvature residual
follows a power law in the deleted capability mass $m_c(\dens)$:
\begin{equation}
\Delta\Lc{c}(\dens)\;=\;\underbrace{\gbar{c}^{\top}\dw_\dens}_{\text{measured}}
\;+\;B\,\big[\underbrace{m_c(\dens)}_{\text{measured}}\big]^{\gamma},
\label{eq:fitlaw}
\end{equation}
with in-sample $R^2 = 0.82$--$0.996$ over 24 capability$\times$model
fits. The held-out battery (1000-resample cell bootstrap) is where the
form is tested: in \emph{leave-one-family-out} transfer the
shared-$\gamma$ mechanism form attains MAE $2.17$ nats versus $10.3$
for per-capability raw sparsity and $8.2$ for the direct-damage
baseline (both CIs exclude zero), so the deleted-mass coordinate is what
transfers across families; it also beats the direct-damage baseline in
fit-shallow/predict-deep ($3.11$ vs.\ $4.24$) and raw sparsity in
leave-largest-model-out ($1.41$ vs.\ $2.15$), while its edge over raw
sparsity within-model does not reach significance
(without the first-order term, QA is unfittable, $R^2\!\le\!0.67$;
with it, QA joins the law). Two structural results:
(i) in the deleted-mass coordinate the exponent concentrates at
$\gamma\in[1.2,2.2]$ across all families, versus $3.2$--$8.7$ in the
raw $(1-\dens)$ coordinate, which is what makes
the form portable; and (ii) \emph{within a model, $\gamma$ is shared
across capabilities} (e.g.\ Qwen3-0.6B: $1.41/1.56/1.43$;
Gemma3-4B: $1.66/1.71$): capability differences appear to be carried
by the two measured quantities, and the fitted structure is two
parameters per model. This mechanism form uses gradient
information of the dense model and is therefore not a K0 predictor; it
is reported as an upper comparator for the reduced forms of the main
text, not as a bound.

\subsection{The signed first-order term predicts, in both directions}
\label{sec:mech}

The alignment term $\gbar{c}^{\top}\dw$ is the family
coefficient that carries sign. Its sign predicts the presence or absence of the
capability-selective improvement in each case measured:
the QA term of Qwen3 is negative and QA improves under pruning (predicted
$-0.83$ vs.\ measured $-0.79$ at 4B, $\dens=0.7$); the QA term of Gemma is
positive and no improvement exists at any size; the term for OLMo-3-7B is
positive (mild degradation, prediction $+0.015$ $=$ measurement at
$\dens=0.8$, code) while the term for OLMo-3-32B is negative, and
deep-pruning QA improvement duly appears. At the level of individual model$\times$capability$\times$density
cells in the mild regime, sign prediction accuracy is $67.6\%$
(Wilson 95\% CI $[58.3,75.7]$, $n{=}108$), above chance, with the
family-level pattern (Qwen and OLMo-32B QA improvements, Gemma
none, an OLMo size flip) called in advance. Beyond the mild regime, first- plus second-order
predictions fail by orders of magnitude at the cliff in each family:
the collapse is non-perturbative in loss space, confirming the
two-regime structure of \eqref{eq:fitlaw}.

\subsection{The cliff is a geometric threshold crossing}
\label{sec:orderparam}

Measuring the capability geometry \emph{of the pruned models}
accounts for the location of the cliff on the three models tested. The
capability-region mass retention (the fraction of the
capability-exclusive top coordinates of the dense model still
prominent after pruning) declines smoothly from the first densities
(no cliff in geometry) while loss is still flat;
the behavioral cliff occurs where retention crosses a critical value.
The critical value is family-specific (Gemma: $0.035$--$0.10$ across
1B/4B and all capabilities; Qwen: $0.13$--$0.23$) but consistent
within a family across sizes and capabilities: the cliff boundary
$\dens_c^{*}(N_0,f)$ reduces to one critical-retention constant per
family, turning the least elegant fitted parameter of the two-regime
law into a measurable, smoothly evolving order parameter.
\preliminary{three models, two families; a multi-model extension is left to future work}

\FloatBarrier
\section{Recovery and Cross-Method Analyses}
\label{sec:app_cross}
\subsection{Recovery arm: redeemable damage and the fitted plateau}
\label{sec:recovery}

Recovery training on general text fits the recovery law cleanly
(Gemma3-270M pruned to $\dens=0.6$; $R^2\approx1.0$):
$\beta'_{\text{math}}=1.23$, $\beta'_{\text{code}}=1.02$, with
fitted plateaus under the fixed LoRA recipe and the largest measured
budget of 16M tokens, $\Lc{c}^{\text{plateau}}-\Lc{c}^{\text{dense}}$,
of $1.21$ (math) and $1.93$ (code) nats; about $80\%$ of this pruning
damage is redeemed within 8M tokens and the remainder persists up to
the largest budget measured under this recipe, while QA recovers
completely. The plateau is a fitted quantity at 16M tokens, not an
established asymptote, and the saturating form does not describe the
non-monotone aligned-data trajectory below. \preliminary{one model, one density, one recipe; with three-point
ladders the largest-budget extrapolation of the saturating form beats a
carry-forward baseline in only 4 of 9 fits, so the ladder is too sparse
to claim extrapolation, and a denser multi-model grid is left to future work} Two mechanism results:
recovery on \emph{aligned} data (teacher traces) heals substantially
faster at small budgets (math $2.76$ vs.\ $3.47$ at $0.5$M tokens),
as the gradient-overlap prediction requires, but over-training on
narrow aligned data \emph{re-damages} the model (QA $4.95\to9.75$
from $0.5$M to $8$M tokens), while general-text recovery is monotone.
Aligned recovery is fast at small budgets and harmful when over-trained on narrow data; general recovery is slow and monotone on this model. Quantization damage past the bit cliff is markedly less redeemable
under the same recipe (residuals $\approx2.6$--$2.8$ after int3) than
pruning damage at matched severity.

\subsection{Resource-ratio models and method selection}
\paragraph{Resource-ratio models.} A method-agnostic response in the
storage or active-parameter ratio was tested on the heterogeneous
panel: at the common storage ratio $0.5$, pruning to $d=0.5$ costs
median $+5.9$, $+8.2$, and $+2.9$ nats while int8 quantization costs
none; over 75 storage-matched pairs (14 with both sides pre-cliff) the
mean absolute method gap is $9.3$ nats with sign agreement $0.61$, and
the two shared-law hypotheses fit on pre-cliff cells reach held-out
sign accuracies of $0.51$ and $0.37$. These models are rejected;
whether another single form could unify the arms is untested. A
capability- and cost-aware method selection would use predictors like
these as loss constraints; an offline replay with frozen predictions
quantifies the decision cost of prediction error, but no held-out
selection validation was run, so nothing here is a validated selector.


\subsection{Re-analysis of an earlier ability-space grid}

A prior internal study fit compression laws directly on Rasch
abilities from $\sim$100-item panels over a 774-cell Qwen3 grid and
reported that no derived law had held-out predictive power. Our
re-analysis attributes much of this to the measurement layer:
(1) the $r{=}1$ falsification anchor was driven almost entirely by the
answer-only recipe (stratified, the anchor holds and held-out error
drops from $9.0\times$ to $1.8\times$ the noise floor); (2)
same-benchmark evaluation inflated QA gains by roughly one third;
(3) discrimination heterogeneity voids interval-scale ability while
rankings survive ($\rho\ge0.98$); (4) item-level damage is smooth
pre-cliff, ruling out discrete skill deletion at the item level.
These findings fixed the protocol of \S\ref{sec:loss}.


\section{Reproducibility and Prediction Registration}
\label{sec:app_registration}
\subsection{Registration and provenance codes}
\label{sec:app_register}
Each prediction test in Table~\ref{tab:pred_main} follows one order. The
functional forms, the development set, and each standardization and
coefficient are fixed and committed first. For a new source the dense
capability loss is then measured, since it is a permitted
pre-compression input; the predictions for each configuration and
candidate are written to a versioned file and committed to the
repository, with the commit hash recorded; and only then is the
compressed or distilled outcome measured. Information budgets are
reported separately: K0 uses pre-compression information only, K1 adds
one pre-specified target-side calibration point, and post-hoc rescaling
on target outcomes is reported only as an oracle diagnostic. Each comparison carries the four-part origin code defined with
Table~\ref{tab:pred_main} and Appendix~\ref{sec:app_results}; the label of a
frozen prediction never depends on its outcome. Register files (paths
in the repository; content hashes in \texttt{docs/MANUSCRIPT\_REWRITE\_MAP.md}):
\texttt{v36b-input-comparison/summary.json} (leave-one-step-out
source axis, both arms), \texttt{v38-prospective/register.json} (frozen
step-96k coefficients), \texttt{v40-prune-strength/register.json}
(shared power form and source-free curves),
\texttt{v41-distill-newpool/summary.json} (the four frozen pool forms),
\texttt{v46-p1-newsource/predictions\_frozen.json} (1B@96k),
\texttt{v49-p1v2/register.json} and
\texttt{predictions\_*.json} (1B and 6.9B pairs, protocols A and B).
When a diagnostic overturns an earlier headline, the earlier claim is
downgraded in the text and the prediction itself stays as committed.

\subsection{Information budgets}
\begin{table}[t]
\centering\small
\begin{tabular}{@{}p{2.5cm}p{5.7cm}p{4.9cm}@{}}
\toprule
Budget & Information available & Cost to disclose \\
\midrule
K0, metadata only & $N_0$, public $D_0$, architecture, intervention
configuration & metadata availability; $D_0$ may be undisclosed \\
K0 + dense anchor & the above plus the dense capability losses
$\mathbf L_0$, measured once before compression & probe count, one
forward pass per probe, native loss units \\
K1 & the above plus one pre-specified measurement of the target at one
compression configuration & the calibration configuration and its
compression cost \\
Oracle diagnostic & post-hoc rescaling on the target outcomes & none;
diagnoses form misfit, never a prediction \\
\bottomrule
\end{tabular}
\caption{Information budgets. Unknown $D_0$ stays unknown; handling of
missing inputs is fixed on the development panel and never inferred
from the target response. Predicting $\mathbf L_0$ from $(N_0,D_0)$ is
the task of dense scaling laws and is not attempted here.}
\label{tab:budgets}
\end{table}

\subsection{Reproduction}
\label{sec:app_repro}
All tables are generated from machine-readable result files by
CPU-only scripts that run in seconds and require neither model weights
nor a GPU; the repository mirrors the scripts and the result files
they read, and the main prediction table is produced by a single
command. Model measurements are reproducible from the recorded
revisions, probe seeds, and protocols above.

\FloatBarrier